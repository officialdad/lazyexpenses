node_modules/
.superpowers/
web/node_modules/
web/.svelte-kit/
web/build/
web/.env*
*.pyc
__pycache__/
.venv/
venv/

# raw bank statements & generated artifacts — never commit
cc-statements/
audit-shots/
# generated pipeline CSVs (reproducible from parse.py / insights.py)
/*.csv
# per-PDF parse cache (reproducible; keyed by content hash)
cache/
# local category overrides (#82); the real one lives on the PVC, not in the repo
/cats.json

# personal financial data — embedded transactions/balances (never commit)
dashboard.html
web/static/data/app.json
# same payload written at the repo root by an older export path — the line above
# is path-anchored and does NOT match it
/app.json
# internal planning docs (real spend figures) — keep local only
docs/superpowers/

# local deployment config - real secrets
.env
# optional local-HTTPS rig (docs/DEPLOY.md): your own CA + leaf key, never committed
local-https/
compose.override.yaml

# agent worktrees - four checkouts of this repo, never committed
.claude/worktrees/
